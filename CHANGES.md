# Отклонения от оригинальной логики RHYMER.EXE

Этот клон — НЕ полная бит-в-бит реимплементация. Подмножество поведения
программы восстановлено по дизассемблированию RHYMER.EXE (Turbo-C, 1991,
31 723 байт), форматы файлов восстановлены по бинарной структуре, но
некоторые детали алгоритма упрощены или заменены — потому что полная
обратная разработка заняла бы месяцы, а оригинал местами намеренно
«глуп» (берёт любое подходящее слово).

Документ фиксирует все сознательные и непреднамеренные отклонения.

---

## Что клонировано ПОЛНОСТЬЮ (проверено побайтно)

| аспект | оригинал | клон |
|--------|----------|------|
| Формат записей словаря `.dat` (CP866, 42 байта, CRLF) | ✅ | ✅ |
| Формат конфига `.cfg` (35 + 35 + 25·N байт, CP866) | ✅ | ✅ |
| Схема ударений: `-` безударный, `'` ударный | ✅ | ✅ |
| Кодировка: CP866 (поддерживается и UTF-8 на входе) | только CP866 | + UTF-8 |
| Ключ рифмы: 5 знаков, цифры + буква | ✅ | ✅ |
| Часть речи: 0–2 знака из {N, A, VН, VП, ?V, ?, пусто} | ✅ | ✅ |
| Многословное заполнение строки (слов больше одного) | ✅ | ✅ |
| Группировка рифм (одна буква рифмы — общий ключ) | ✅ | ✅ |
| Запрет «то же слово в строке» (между строк — sliding window) | скорее всего ✅ | ✅ (24-окно) |
| Случайное зерно из BIOS-таймера при запуске | ✅ | ✅ (через Date.now) |
| Лимит глубины разложения = 8 слов | ✅ | ✅ |
| Лимит длины префикса = 8 слогов | ✅ | ✅ |
| Нельзя два одинаковых слова подряд | ✅ | ✅ |
| Проверка POS: после прилагательного должно идти существительное | ✅ | ✅ |
| Проверка POS: однословная строка не должна быть наречием | ✅ | ✅ |

## Что УПРОЩЕНО / ЗАМЕНЕНО

### 1. POS-фильтр

**Оригинал** (диспетчер 0x2cd6): таблица из 4 веток для первого слова строки:
- `?` (наречие/частица) → отдельная ветка с проверками позиций
- `A` (прилагательное) → ищет последнее прилагательное, после него должно быть сущ.
- `N` (существительное) → если есть verb в начале, недопустимо в строке из 2 слов
- `V` (глагол) → особые проверки на причастия (VН / VП)

**Клон**: грубый фильтр `passesPosFilter()` — однословные строки не могут быть
наречием; после прилагательного должно идти существительное. Не различает
ветки по первой букве строки.

### 2. Рофма

**Оригинал**: последнее слово строки должно иметь rhyme-ключ, равный
зафиксированному якорю группы. Это жёсткое требование, при отсутствии
вариантов строка возвращает пустоту.

**Клон**: «мягкая» рифма — если вариантов с якорем нет, берётся любой
вариант. Это сделано потому, что словарь мал (445 слов) и строгий лок
оставлял бы много пустых строк.

### 3. Выбор слов

**Оригинал**: предвычисленные слоты в массиве `[0x1940]` (до 21 слова на
строку). Алгоритм заполняет каждый слот случайным словом, подходящим по
шаблону, и проверяет POS-фильтр по позиции.

**Клон**: динамический бэктрекинг с рандомизированным обходом. Перемешивает
префиксы и кандидатов на каждом уровне. Делает cap = 200 декомпозиций,
лимит = 4 на первое слово (для разнообразия). В целом поведение похожее, но
не идентичное.

### 4. Словарь `.res` (резервные рифмы)

**Оригинал**: использует файл `rhymer.res` (8 960 байт) как источник
готовых рифмованных пар. Когда не удаётся сгенерировать, подставляет
заготовленные стихи (знаменитое «марксизм / троцкизм» из этого файла).

**Клон**: файл `rhymer.res` НЕ парсится. Однако некоторые слова из
него (`марксизм`, `троцкизм`, `большевизм`, `коммунизм` и др.) присутствуют
в `rhymer.dat`, поэтому изредка всплывают и в клоне (см. пример в README).

### 5. Защита PGPROT / CRC-проверка

**Оригинал**: при старте проверяет CRC первых 47 байт кода. Если CRC не
совпадает с эталоном — выдаёт длинное сообщение о возможном вирусе и
завершает работу (видимо, тело программы модифицировано упаковщиком
PGPROT, и при его срабатывании CRC слетает).

**Клон**: не клонируется. Неактуально для веба и не влияет на логику
генерации.

### 6. TUI (текстовый псевдографический интерфейс)

**Оригинал**: полноэкранный DOS-интерфейс с псевдографикой ╔══╗ ║ ╚══╝,
горячими клавишами F3–F10, навигацией стрелками.

**Клон**: HTML-форма с теми же командами (F3 запомнить строку, F4 вспомнить,
F5 сохранить, F6 загрузить, F7 вставить строку, F8 удалить, F9 сочинить,
F10 выход) — но через кнопки.

### 7. Работа с памятью

**Оригинал**: типичная для DOS — релокации, PSP, перехваты int 21h/10h/1Ah,
работа с тейл-памятью через MCB. Использует Turbo-C runtime 1988.

**Клон**: не клонируется. Неактуально для веба.

### 8. Защита от одинаковых слов между строк

**Оригинал**: предположительно запрещает повторы в одной позиции (slot index)
словаря. Точное поведение не восстановлено.

**Клон**: скользящее окно последних 24 использованных слов. Слово не
появляется снова в ближайших 24 употреблениях. Это мягче оригинала.

### 9. RNG

**Оригинал**: LCG, зерно из BIOS int 1Ah (cx:dx пара регистров).

**Клон**: тот же LCG (константы 1103515245, 12345 — POSIX-стандарт),
зерно из `Date.now() ^ (Math.random() * 0x7fffffff)`.

## Что НЕ клонировано СОЗНАТЕЛЬНО

- DOS-обёртка (PSP, релокации, INT-обработчики)
- Turbo-C runtime 1988 (обработка ошибок, RTL-функции)
- Сжатие `.EXE` через PGPROT
- Команда `F10 — выход` (в браузере бесполезна; закрытие вкладки достаточно)

## Известные косяки клона

1. При малом словаре и жёстком rhyme-lock половина строк становится `[нет: …]`.
   Мягкая рифма (см. п.2 выше) смягчает это, но не устраняет.
2. POS-фильтр пропускает строки типа «красивый шагнул далеко» — прилагательное
   + глагол в одной строке, что в оригинале отсекалось бы веткой `A`.
3. Алгоритм выбирает максимально «плотные» декомпозиции (меньше слов),
   из-за чего первая версия клона выдавала «не шаг не шаг не шаг не знает».
   Поправлено лимитом `perFirstCap=4` — теперь видно разнообразие.
